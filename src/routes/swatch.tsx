import { Layout } from '@/components/layout';
import { useDocumentTitle } from '@uidotdev/usehooks';

export default function Swatch() {
  useDocumentTitle('Palletone | Swatch');
  
  return (
    <Layout>
      <main></main>
    </Layout>
  );
}
